@model My3Common.Role

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/Content/RoleDetailsForm.css">
    <title>Delete Role</title>
</head>
<body>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h3>Are you sure you want to delete this Role?</h3>
            <hr />
            <br />
            <div>

                @Html.LabelFor(model => model.Name, "Name", htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @readonly = "readonly" } })
                </div>


                @Html.LabelFor(model => model.Code, "Code", htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.Code, new { htmlAttributes = new { @readonly = "readonly" } })
                </div>

               
                <div class="form-actions no-color">
                    <input type="submit" value="Delete" onclick="location.href='@Url.Action("Delete","Role",new { id = Model.ID })'" class="btn btn-default" />
                    <input type="button" value="Back to List" onclick="location.href='@Url.Action("Roles","Admin")'" class="btn btn-default" />
                </div>
            </div>
        </div>
    }


</body>
</html>
